// Keeping crypto seperate even though it may employ the example algorithms

const run = async () => {
  console.log('Crypto example running');
};

const name = () => 'Crypto';

module.exports = {run, name};
